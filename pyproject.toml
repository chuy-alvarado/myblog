[project]
name = "Chuch-blog"
version = "0.1.0"
description = "Chuch' blog project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0",
    "django-rest-framework>=0.1.0",
    "markdown>=3.10",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-django>=4.11.1",
]
